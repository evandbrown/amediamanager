packages: 
  python:
    awscli: "1.1.0"
container_commands:
  asset_sync:
    command: |
      aws s3 sync --region `{ "Ref" : "AWS::Region"}` --acl public-read static s3://$S3_UPLOAD_BUCKET/static/
    leader_only : true